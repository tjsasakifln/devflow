# Story 3.2: Adversarial Verify Pattern

**Status:** Done
**Epic:** 3 — Multi-Agent Orchestration

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "tsc"]

## Story

As a **developer**,
I want **findings from any analysis to be adversarially verified by independent agents**,
so that **false positives are caught before they become tasks**.

## Scope

**IN:** Pattern de verificação adversarial: N findings → N verifiers independentes, lenses (correctness, security, repro), threshold >=2 de 3, integração com `adversarial-review`.
**OUT:** Adversarial com LLM (Story 4.3).

**Complexity:** M (3 verifiers por finding, fan-out/fan-in)

## Acceptance Criteria

1. Pattern: N findings → spawn N verifiers → cada um tenta REFUTAR
2. Finding sobrevive se >=2 de 3 verificadores independentes não refutam (split decision 2-1 = "disputed", escalado para revisão humana)
3. Verifiers usam lenses diferentes: correctness, security, repro
4. Integrado ao `devflow adversarial-review` existente

## Integration Verification

1. IV1: Finding falso-positivo → refutado por >=2 verifiers → removido
2. IV2: Finding real → sobrevive a 3 verifiers → mantido
3. IV3: Verificação não adiciona >2x ao tempo da análise original

## Tasks / Subtasks

- [x] 1. Implementar `src/kernel/orchestration/adversarial-verify.ts`
- [x] 2. Implementar spawn de N verifiers para cada finding (usa Story 3.1)
- [x] 3. Implementar 3 lenses: correctness (lógica correta?), security (vulnerabilidade?), repro (reproduzível?)
- [x] 4. Implementar threshold: >=2 de 3 = sobrevive, 2-1 = "disputed", <=1 = refutado
- [x] 5. Integrar com `src/commands/adversarial-review.ts` existente
- [x] 6. Adicionar flag `--verify-mode=adversarial` ao comando existente
- [x] 7. Tratar split decisions: findings "disputed" vão para revisão humana

## Dev Notes

- Usa Story 3.1 (Parallel Agent Spawner) como dependência
- Integração com `adversarial-review` existente: os 12 vetores atuais são determinísticos, verify pattern adiciona camada multi-agente
- Cada verifier é um agente independente com prompt focado em REFUTAR (não confirmar)
- Lenses são mutuamente exclusivas: correctness verifier não vê security concerns, etc.

## Risks

Médio — falso-positivo sobrevivendo a 3 verifiers (raro mas possível). Mitigação: findings com veredit 2-1 (split decision) são marcados como "disputed" e escalados para revisão humana.

## Testing

- Teste com finding falso-positivo conhecido → verificar refutação
- Teste com finding real conhecido → verificar sobrevivência
- Teste de split decision (2-1) → verificar marcação "disputed"
- Teste de integração: `devflow adversarial-review <id> --verify-mode=adversarial`

## File List

| File | Action |
|------|--------|
| `src/kernel/orchestration/types.ts` | modified — added adversarial types (AdversarialLens, AdversarialVerdict, VerificationResult, VerificationConfig, AdversarialVerificationResult, DisputedFindingEntry) |
| `src/kernel/orchestration/adversarial-verify.ts` | created — AdversarialVerifier class with 3 lenses (correctness, security, repro), threshold logic, dispute persistence |
| `src/commands/adversarial-review.ts` | modified — added --verify-mode=adversarial option, adversarial verification integration |
| `src/cli/index.ts` | modified — added --verify-mode option to adversarial-review command |
| `test/unit/adversarial-verify.test.ts` | created — 39 tests covering all lenses, threshold, IV1/IV2/IV3 integration scenarios |

## 🤖 CodeRabbit Integration

**CodeRabbit Integration**: Disabled

## QA Results

### Review Date: 2026-07-08

### Reviewed By: Quinn (Guardian)

**7-Point QA Checklist:**
1. **Code review** -- PASS. Clean architecture with single AdversarialVerifier class, 3 clearly separated lenses, worker pool pattern consistent with Story 3.1, dispute persistence with append logic.
2. **Unit tests** -- PASS. 39 tests all passing. Covers: basic structure, multi-finding, all 3 lenses (correctness/security/repro), threshold logic (>=2/3 survive, 2-1 disputed, <=1 refuted), IV1/IV2/IV3 scenarios, edge cases.
3. **Acceptance criteria** -- PASS. All 4 ACs met: N findings -> N verifiers attempting refutation, threshold logic, 3 lenses, integration with `adversarial-review --verify-mode=adversarial`.
4. **No regressions** -- PASS. 654/655 tests pass (1 pre-existing timeout in audit-engine.test.ts, unrelated).
5. **Performance** -- PASS. Concurrent worker pool, parallel lens verifiers per finding. IV3 requirement (<2x original time) structurally satisfied.
6. **Security** -- PASS. No injection vectors. Dispute data written to controlled path.
7. **Documentation** -- PASS. Well-documented source, comprehensive story file.

### Gate Status

Gate: PASS -> docs/qa/gates/3.2-adversarial-verify-pattern.yml

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 1.0.0 | Validated GO (7/10) — Status: Draft -> Ready | @po |
| 2026-07-08 | 1.1.0 | Development started (yolo mode) — Status: Ready -> InProgress | @dev |
| 2026-07-08 | 1.1.0 | Development complete — Status: InProgress -> InReview | @dev |
| 2026-07-08 | 1.1.1 | QA Gate PASS — Status: InReview -> Done | @qa |
