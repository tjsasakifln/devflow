# Story 2.4: Agent-Driven Development Workflow

**Status:** Done
**Epic:** 2 — Universal Workflow Engine

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "tsc"]

## Story

As a **developer**,
I want **workflow steps that delegate to specialized AI agents with authority boundaries**,
so that **complex development tasks are handled by the right agent at the right time**.

## Scope

**IN:** Agent delegation matrix no engine (PM→SM→PO→Dev→QA→DevOps), tags `agent: <role>` nos steps, handoff protocol (<500 tokens), worktree isolation.
**OUT:** Multi-agent paralelo (Epic 3).

**Complexity:** M (integração de conceitos existentes: handoff protocol do AIOX + engine da Story 2.2)

## Acceptance Criteria

1. Agent delegation matrix implementada no engine (PM→SM→PO→Dev→QA→DevOps)
2. Cada step do workflow pode ser tagged com `agent: <role>` — engine spawna agente correto
3. Handoff protocol entre agentes usando artifact compacto (<500 tokens)
4. Agentes são subprocessos isolados (worktree git opcional)
5. Agent authority enforced: Dev não pode push, QA não pode implement, etc.

## Integration Verification

1. IV1: Workflow com múltiplos agentes executado sem quebra de autoridade
2. IV2: Handoff artifact gerado entre cada transição de agente
3. IV3: Agente sem autoridade para operação → bloqueado com mensagem clara

## Tasks / Subtasks

- [x] 1. Implementar agent delegation matrix no engine (referência: `.claude/rules/agent-authority.md`)
- [x] 2. Adicionar suporte a tags `agent: <role>` no workflow state machine spec
- [x] 3. Implementar spawn de agente como subprocesso isolado
- [x] 4. Implementar worktree isolation (opcional, fallback para subprocesso simples)
- [x] 5. Implementar handoff protocol (artifact YAML <500 tokens)
- [x] 6. Implementar enforcement de autoridade: operações bloqueadas por role
- [x] 7. Testar fluxo multi-agente: PM cria epic → SM cria story → PO valida → Dev implementa

## Dev Notes

- Handoff protocol definido em `.claude/rules/agent-handoff.md`
- Agent authority matrix em `.claude/rules/agent-authority.md`
- Worktree isolation usa `git worktree add` (~200-500ms) — opcional
- Subprocesso simples usa `child_process.spawn` com contexto passado via stdin/env
- Handoff artifacts seguem formato: `from_agent`, `to_agent`, `story_context`, `decisions`, `files_modified`, `blockers`, `next_action`

## Risks

Médio — worktree isolation é custoso (~200-500ms setup). Mitigação: opcional, fallback para subprocesso simples.

## Testing

- Teste unitário: agent spawn com mock de subprocesso
- Teste de autoridade: Dev tenta push → bloqueado
- Teste de handoff: artifact gerado contém todos os campos obrigatórios
- Teste E2E: fluxo multi-agente mínimo (2 agentes)

## File List

| File | Action |
|------|--------|
| `src/kernel/workflow/agent-delegation.ts` | Created |
| `src/kernel/workflow/agent-spawner.ts` | Created |
| `src/kernel/workflow/handoff.ts` | Created |
| `src/kernel/workflow/authority-enforcer.ts` | Created |
| `src/kernel/workflow/types.ts` | Extended (AgentRole, HandoffArtifact, agent field on TransitionDef, currentAgent/previousAgent on EngineState) |
| `src/kernel/workflow/engine.ts` | Extended (agent-tagged transition execution, executeAgentTransition, agent accessors) |
| `src/kernel/workflow/persistence.ts` | Extended (agent fields in createDefaultEngineState, migrateV1toV2, v2 backward compat) |
| `test/unit/workflow-agent-delegation.test.ts` | Created |

## 🤖 CodeRabbit Integration

**CodeRabbit Integration**: Disabled

## QA Results

### Review Date: 2026-07-08

### Reviewed By: Quinn (Guardian)

**Verdict: PASS** — Agent-driven workflow fully implemented. Delegation matrix (10 roles), agent-tagged transitions, handoff protocol (<500 tokens with enforcement), authority enforcement (default-deny with explicit blocked/delegate-only/allowed), subprocess spawning with optional worktree isolation all verified. 742 tests pass. TypeScript compiles clean.

### Gate Status

Gate: PASS -> docs/qa/gates/2.4-agent-driven-development-workflow.yaml

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 1.0.0 | Validated GO (7/10) — Status: Draft -> Ready | @po |
| 2026-07-08 | 1.1.0 | Implementation complete — Status: Ready -> InProgress -> InReview | @dev |
| 2026-07-08 | 1.1.1 | QA Gate PASS — Status: InReview -> Done | @qa |
