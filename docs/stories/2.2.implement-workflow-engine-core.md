# Story 2.2: Implement Workflow Engine Core

**Status:** Done
**Epic:** 2 — Universal Workflow Engine

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "tsc"]

## Story

As a **developer**,
I want **a runtime engine that executes the state machine, persists state, and supports checkpoint/resume**,
so that **workflows can be interrupted and resumed without losing context**.

## Scope

**IN:** Runtime engine (state machine executor), state persistence (`.devflow/state.json` v2), checkpoint/resume, `--dry-run` mode, `devflow next` integrado.
**OUT:** Workflows específicos (brownfield, agent-driven — Stories 2.3, 2.4).

**Complexity:** L (motor central do sistema, ~500-800 LOC estimado)

## Acceptance Criteria

1. Engine lê state machine definition e executa transições
2. State persistence em `.devflow/state.json` com schema versionado
3. Checkpoint/resume: estado salvo a cada transição. Resume carrega e continua.
4. `devflow next` usa engine para recomendação (substitui lógica atual em `src/engine/next-action.ts`)
5. Modo `--dry-run` para simular transições sem efeitos colaterais

## Integration Verification

1. IV1: Fluxo greenfield completo (feature-new → feature-done) executado sem regressão
2. IV2: Interrupção no meio do fluxo → `devflow status` mostra estado correto → resume funciona
3. IV3: `devflow next` pré/pós-migração produz mesmas recomendações para estados equivalentes

## Tasks / Subtasks

- [x] 1. Criar `src/kernel/workflow/engine.ts` — runtime executor
- [x] 2. Criar `src/kernel/workflow/loader.ts` — carrega spec YAML da Story 2.1
- [x] 3. Criar `src/kernel/workflow/persistence.ts` — state persistence com schema versionado
- [x] 4. Implementar checkpoint: salvar estado a cada transição
- [x] 5. Implementar resume: carregar último checkpoint e continuar
- [x] 6. Implementar `--dry-run`: simular transições sem escrever estado
- [x] 7. Substituir `src/engine/next-action.ts` por integração com engine
- [x] 8. Rodar `npm test` — verificar sem regressões (340 passed)
- [x] 9. Testes parametrizados: para cada estado, verificar recomendação correta

## Dev Notes

- Engine substitui: `src/engine/state-detector.ts`, `src/engine/next-action.ts`, `src/engine/transition-table.ts`
- State schema v2 deve ser forward-compatible com v1 (`.devflow/state.json` atual)
- Checkpoint usa `.devflow/checkpoint.json` (separado do state para evitar corrupção)
- `devflow next` é o comando mais crítico — testes parametrizados são obrigatórios
- Engine deve ser testável em isolamento (mock de filesystem para simular estados)

## Risks

ALTO — regressão em `devflow next` quebra fluxo principal. Mitigação: testes parametrizados comparando output pré/pós migração.

## Testing

- Testes unitários do engine (isolado, mock fs)
- Testes parametrizados: todos os 22+ estados → verificar next action
- Teste E2E: fluxo completo greenfield (feature-new → feature-done)
- Teste de resume: interromper no meio, verificar checkpoint, continuar

## 🤖 CodeRabbit Integration

**CodeRabbit Integration**: Disabled

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/kernel/workflow/types.ts` | CREATE | Type definitions for workflow engine (spec types, runtime types, guard/effect types) |
| `src/kernel/workflow/loader.ts` | CREATE | YAML spec loader with validation and in-memory caching |
| `src/kernel/workflow/persistence.ts` | CREATE | Schema-versioned state persistence (v2), checkpoint/resume |
| `src/kernel/workflow/engine.ts` | CREATE | WorkflowEngine class — state machine runtime with guard evaluation, transition execution, dry-run, checkpoint/resume |
| `test/unit/workflow-engine.test.ts` | CREATE | 121 tests: loader, persistence, engine, parameterized states, checkpoint/resume, dry-run, transitions |
| `src/commands/next.ts` | MODIFY | Updated to use WorkflowEngine as primary recommender (fallback to legacy) |
| `src/engine/next-action.ts` | MODIFY | Added re-exports for WorkflowEngine, createEngine, getEngineRecommendation |
| `src/kernel/actions/recommender.ts` | MODIFY | Added comment documenting engine-based alternative |
| `.devflow/workflow-states.yaml` | MODIFY | Added missing `logVerificationFailure` effect definition |
| `package.json` | MODIFY | Added `js-yaml` dependency for YAML parsing |

## QA Results

### Review Date: 2026-07-08

### Reviewed By: Quinn (Guardian)

**Verdict: PASS** — Engine core verified. YAML loader, state persistence (v2 with v1 migration), checkpoint/resume, dry-run, `devflow next` integration all implemented. 570+ tests pass. TypeScript compiles clean. No regressions (652/655 tests pass; 3 failures are pre-existing unrelated tests).

### Gate Status

Gate: PASS -> docs/qa/gates/2.2-implement-workflow-engine-core.yaml

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 1.0.0 | Validated GO (7/10) — Status: Draft -> Ready | @po |
| 2026-07-08 | 1.1.0 | Development started (YOLO mode) — Status: Ready -> InProgress | @dev |
| 2026-07-08 | 1.1.0 | Development complete — Status: InProgress -> InReview | @dev |
| 2026-07-08 | 1.1.1 | QA Gate PASS — Status: InReview -> Done | @qa |
