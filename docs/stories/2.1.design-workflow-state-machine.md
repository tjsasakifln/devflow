# Story 2.1: Design Workflow State Machine

**Status:** Done
**Epic:** 2 — Universal Workflow Engine

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "tsc"]

## Story

As a **developer**,
I want **a unified state machine definition that covers all 4 workflow types**,
so that **every project state and transition is explicitly modeled and testable**.

## Scope

**IN:** Documento de design da state machine (DSL ou YAML), 22 estados existentes mapeados, novos estados brownfield, visualização Mermaid.
**OUT:** Implementação do engine (Story 2.2).

**Complexity:** M (design puro, sem código, mas requer análise completa dos 22 estados)

## Acceptance Criteria

1. State machine DSL ou config file que define: estados, transições, guards, efeitos
2. 22 estados existentes mapeados + novos estados brownfield
3. Transições cobrem: greenfield (15 estados feature pipeline), brownfield (discovery → epic → story → implement → verify), bugfix (simplificado), review (QA loop)
4. Cada transição tem: condição de guarda, efeito colateral, próximo estado
5. Visualização da state machine gerável (Mermaid/Graphviz)

## Integration Verification

1. IV1: State machine spec validada contra todos os fluxos documentados
2. IV2: `devflow next` para cada estado produz recomendação correta (teste parametrizado)

## Tasks / Subtasks

- [x] 1. Analisar `src/engine/transition-table.ts` e `src/engine/state-detector.ts` — documentar estados e transições atuais
- [x] 2. Mapear os 22 estados existentes (`src/kernel/types/state.ts`) — na realidade 26 estados (23 ativos + 3 legacy) encontrados e mapeados
- [x] 3. Adicionar novos estados brownfield: discovery-scout, discovery-archaeologist, discovery-detective, discovery-architect, discovery-writer
- [x] 4. Definir transições para bugfix workflow (simplificado: detect → implement → verify → done) — 4 estados, 9 transições
- [x] 5. Definir transições para review/QA loop (in-review → qa-pass → done, in-review → qa-fail → in-progress) — guardas condicionais nas transições t040-t044
- [x] 6. Criar spec file: `.devflow/workflow-states.yaml` com DSL completo — 35 estados, 73 transições, 52 guards, 38 effects
- [x] 7. Gerar visualização Mermaid e validar com time — `.devflow/workflow-states.mmd` criado, validação cruzada de referências OK

## Dev Notes

- Estados atuais definidos em `src/kernel/types/state.ts` (22 estados)
- Transições atuais em `src/engine/transition-table.ts`
- `src/engine/state-detector.ts` tem lógica de detecção que será substituída pelo engine na Story 2.2
- Output deste design alimenta diretamente a implementação da Story 2.2
- Referência: padrão LangGraph StateGraph (nodes + edges + conditional edges)
- Formato sugerido para DSL: YAML com `states`, `transitions`, `guards`, `effects`

## Risks

Médio — design incompleto quebra stories subsequentes. Mitigação: validar contra todos os fluxos documentados antes de prosseguir.

## Testing

- Teste parametrizado: para cada estado, `devflow next` produz recomendação esperada
- Validar que todos os 22+ estados têm pelo menos uma transição de saída
- Validar que estados terminais não têm transições de saída inválidas

## 🤖 CodeRabbit Integration

**CodeRabbit Integration**: Disabled

> Story type: Design (spec/document). No executable code — design artifact only.

## QA Results

### Review Date: 2026-07-08

### Reviewed By: Quinn (Guardian)

**Verdict: PASS** — Design artifact validated. All 35 states, 73 transitions, 52 guards, 39 effects verified. Cross-references clean. Mermaid visualization complete. Minor DOC-001: story claims 38 effects (actual: 39 — `logVerificationFailure` added in Story 2.2).

### Gate Status

Gate: PASS -> docs/qa/gates/2.1-design-workflow-state-machine.yaml

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 1.0.0 | Validated GO (7/10) — Status: Draft → Ready | @po |
| 2026-07-08 | 2.0.0 | Development started (YOLO mode) — Status: Ready → InProgress | @dev |
| 2026-07-08 | 2.0.0 | Development complete — Status: InProgress → InReview | @dev |
| 2026-07-08 | 2.0.1 | QA Gate PASS — Status: InReview -> Done | @qa |

## File List

- `.devflow/workflow-states.yaml` — Created. Complete DSL spec: 35 states, 73 transitions, 52 guard conditions, 38 effects. Covers greenfield, brownfield, bugfix, and QA loop workflows.
- `.devflow/workflow-states.mmd` — Created. Standalone Mermaid state diagram for rendering in mermaid.live or Markdown viewers.

## Dev Agent Record

### Agent Model Used

- **Primary:** deepseek-v4-flash (implementation agent)
- **Mode:** YOLO (autonomous)
- **Agent:** Dex (Builder)

### Completion Notes

- Design-only story: no executable code produced
- 26 existing states were analyzed and documented, plus 9 new states added (5 discovery + 4 bugfix)
- 73 transitions defined with explicit guard conditions and effects
- All 4 workflow types covered: greenfield (project pipeline), brownfield (discovery + pipeline), bugfix (simplified shortcut), QA loop (conditional review gates)
- YAML spec is ready to feed directly into Story 2.2 (engine implementation)
- Validation: all state references cross-checked, all guard/effect references verified
- CodeRabbit disabled for this design-only story per story configuration
