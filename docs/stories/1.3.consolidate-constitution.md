# Story 1.3: Consolidate Constitution

**Status:** Done
**Epic:** 1 — Kernel Consolidation

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "tsc"]

## Story

As a **developer**,
I want **constitution logic in `src/kernel/constitution/` only**,
so that **constitutional checks have a single code path**.

## Scope

**IN:** `src/constitution/` (checker.ts, defaults.ts, loader.ts) → `src/kernel/constitution/`.
**OUT:** Nada — remoção total do diretório legado.

**Complexity:** S (3 arquivos, sem nova lógica)

## Acceptance Criteria

1. `src/constitution/` removido — imports redirecionados para `src/kernel/constitution/`
2. 12 artigos constitucionais preservados
3. `devflow doctor` verifica constitution integrity

## Integration Verification

1. IV1: Constitution checker reporta mesmas violações pré/pós-migração
2. IV2: Gatekeep valida constitution compliance identicamente

## Tasks / Subtasks

- [x] 1. Verificar todos os imports de `src/constitution/` no codebase
- [x] 2. Redirecionar imports para `src/kernel/constitution/`
- [x] 3. Remover diretório `src/constitution/`
- [x] 4. Rodar `npm test` e `npm run typecheck`
- [x] 5. Testar `devflow doctor` — verificar constitution integrity check
- [x] 6. Testar `devflow gatekeep <id> --approve` — verificar constitution compliance

## Dev Notes

- Arquivos são idênticos entre `src/constitution/` e `src/kernel/constitution/` (confirmado por diff)
- Constitution checker é usado por: `gatekeep.ts`, `doctor.ts`, `feature-complete.ts`
- 12 artigos definidos em `src/kernel/constitution/defaults.ts`

## Risks

Baixo — arquivos idênticos. Verificar se há imports diretos de `src/constitution/` em comandos ou testes.

## Testing

- `npm test`
- `npm run typecheck`
- `npx devflow doctor` (constitution integrity)
- `npx devflow gatekeep <id> --approve --actor test` (constitution compliance)

## 🤖 CodeRabbit Integration

**CodeRabbit Integration**: Disabled

> Story type: Refactor (migration). Identical files — import path consolidation only.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 1.0.0 | Validated GO (7/10) — Status: Draft → Ready | @po |
| 2026-07-08 | 1.1.0 | Development started (yolo mode) — Status: Ready → InProgress | @dev |
| 2026-07-08 | 1.2.0 | Development complete — Status: InProgress → InReview | @dev |
| 2026-07-08 | 1.2.1 | QA Gate CONCERNS — Status: InReview → Done | @qa |

## File List

- `src/commands/feature-complete.ts` — import path `"../constitution/checker.js"` → `"../kernel/constitution/checker.js"`
- `test/unit/deprecated-imports.test.ts` — test paths updated from `src/constitution/` to `src/kernel/constitution/`
- `src/constitution/checker.ts` — deleted (was re-export shim)
- `src/constitution/defaults.ts` — deleted (was re-export shim)
- `src/constitution/loader.ts` — deleted (was re-export shim)

## QA Results

### Review Date: 2026-07-08

### Reviewed By: Quinn (Guardian)

| Check | Result | Notes |
|-------|--------|-------|
| 1. Code Review | PASS | Clean mechanical import redirection. All 3 constitution files redirect identically to kernel paths. |
| 2. Unit Tests | CONCERNS | 654/655 pass. One pre-existing timeout in `audit-engine.test.ts` (unrelated — file not modified by this story). |
| 3. Acceptance Criteria | PASS | All 3 ACs met: src/constitution/ removed, 12 articles preserved, doctor/gatekeep functional. |
| 4. No Regressions | PASS | All behavioral tests pass. Constitution checker resolves correctly via kernel path. Typecheck clean. |
| 5. Performance | PASS | Import redirect overhead negligible. No new computation introduced. |
| 6. Security | PASS | Constitution checks are validation logic — no security-sensitive code. |
| 7. Documentation | PASS | Story file documents the consolidation. No external documentation impacted. |

### Gate Status

Gate: CONCERNS → docs/qa/gates/1.3-consolidate-constitution.yaml
