# Story 3.3: Completeness Critic Pattern

**Status:** Done
**Epic:** 3 — Multi-Agent Orchestration

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "tsc"]

## Story

As a **developer**,
I want **a final agent that asks "what's missing?" after every analysis**,
so that **no dimension, claim, or source goes unexamined**.

## Scope

**IN:** Critic agent pós-análise, output de gaps (dimensões não cobertas, fontes não lidas, claims não verificadas), loop-until-dry (max 5 iterações), integração com `devflow next --diagnose`.
**OUT:** Correção automática dos gaps (responsabilidade do usuário ou do @dev).

**Complexity:** S (um agente, um padrão de loop)

## Acceptance Criteria

1. Critic agent roda após cada análise (discover, review, audit)
2. Output: lista do que não foi coberto, fontes não lidas, claims não verificadas
3. Output do critic vira input para próxima rodada (loop-until-dry)
4. Dry condition: critic report contém 0 itens não cobertos em 2 rodadas consecutivas — encerra loop
5. Integrado ao `devflow next --diagnose`

## Integration Verification

1. IV1: Após discover, critic identifica módulo não analisado → próxima ação recomendada
2. IV2: Loop-until-dry: N rodadas até critic retornar vazio (max 5)

## Tasks / Subtasks

- [x] 1. Implementar `src/kernel/orchestration/completeness-critic.ts`
- [x] 2. Implementar critic agent com 3 dimensões de análise (dimensions not covered, sources not read, claims not verified)
- [x] 3. Implementar loop-until-dry: rodar critic → alimentar gaps → re-analisar → repetir
- [x] 4. Implementar dry condition: 0 itens em 2 rodadas consecutivas
- [x] 5. Limite máximo de iterações: 5 (evitar loop infinito)
- [x] 6. Integrar com `devflow next --diagnose` (já existe flag)
- [x] 7. Integrar com `devflow discover` (roda critic após análise brownfield)

## Dev Notes

- Usa Story 3.1 (Parallel Agent Spawner) como dependência — critic é um agente especializado
- Loop-until-dry: padrão consagrado em workflows multi-agente (CrewAI, LangGraph)
- Critic NÃO corrige gaps — apenas reporta. Correção é responsabilidade do @dev
- Integração natural com `devflow next --diagnose` (flag já existe, hoje mostra info básica)
- Comportamento de iteração: enriquece contexto entre iterações marcando gaps como cobertos, permitindo que o loop convirja

## Risks

Baixo — critic pode gerar falsos positivos (gaps que não são gaps reais). Mitigação: loop para após 2 rodadas consecutivas com 0 itens não cobertos (dry condition objetiva).

## Testing

- Teste: após discover, verificar output do critic contém dimensões não cobertas
- Teste: loop-until-dry com mock de critic que retorna vazio na 2ª rodada
- Teste: limite de 5 iterações — critic sempre retorna gaps → para no 5º ciclo
- Teste de integração: `devflow next --diagnose` mostra gaps do critic

## File List

### New Files
- `src/kernel/orchestration/completeness-critic.ts` — CompleteCritic class with runCritic(), fullCritique(), and loop-until-dry pattern
- `test/unit/completeness-critic.test.ts` — 12 unit tests covering dimension, source, claim analysis and loop-until-dry behavior

### Modified Files
- `src/kernel/orchestration/types.ts` — Added CriticGap, CriticGapType, CriticIteration, CriticReport, CriticConfig types
- `src/commands/next.ts` — Added CompletenessCritic integration in showDiagnosticDetail (--diagnose flag)
- `src/commands/discover.ts` — Added CompletenessCritic integration after discovery workflow completes

## Implementation Log

| Step | File | Decision | Rationale |
|------|------|----------|-----------|
| Types | `types.ts` | ADAPT | Added critic types alongside existing spawner types |
| Class | `completeness-critic.ts` | CREATE | No existing critic pattern to reuse |
| Test | `test/unit/completeness-critic.test.ts` | CREATE | Follows existing test patterns (vitest, hoisted mocks) |
| Integration | `next.ts` | ADAPT | Critic runs after parallel analysis in --diagnose |
| Integration | `discover.ts` | ADAPT | Critic runs post-discovery as a best-effort analysis |

## QA Results

### Review Date: 2026-07-08

### Reviewed By: Quinn (Guardian)

**7-Point QA Checklist:**
1. **Code review** -- PASS. Clean CompletenessCritic class with loop-until-dry pattern, 3 gap dimensions properly separated, iterative context enrichment via AnalysisContext interface.
2. **Unit tests** -- PASS. 12 tests all passing. Covers: critic instantiation, 3 gap dimension detection, loop-until-dry convergence, max iteration limit, config overrides.
3. **Acceptance criteria** -- PASS. All 5 ACs met: post-analysis critic, 3 gap outputs, loop-until-dry with iterative refinement, dry condition (2 consecutive clean rounds), integration with `devflow next --diagnose`.
4. **No regressions** -- PASS. 654/655 tests pass (1 pre-existing timeout in audit-engine.test.ts, unrelated).
5. **Performance** -- PASS. Max 5 iterations prevents infinite loops. File scanning limited (30 files for markers, 20 for sources). Efficient design.
6. **Security** -- PASS. Respects skip directories. No injection vectors.
7. **Documentation** -- PASS. Well-documented source with implementation log. Comprehensive story file.

### Gate Status

Gate: PASS -> docs/qa/gates/3.3-completeness-critic-pattern.yml

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 1.0.0 | Validated GO (7/10) -- Status: Draft -> Ready | @po |
| 2026-07-08 | 1.0.1 | Status: Ready -> InProgress -- Implementation started | @dev |
| 2026-07-08 | 1.1.0 | CompletenessCritic class implemented with full loop-until-dry | @dev |
| 2026-07-08 | 1.1.0 | Integration with `devflow next --diagnose` completed | @dev |
| 2026-07-08 | 1.1.0 | Integration with `devflow discover` completed | @dev |
| 2026-07-08 | 1.1.0 | 12 unit tests passing, typecheck clean | @dev |
| 2026-07-08 | 1.1.0 | Status: InProgress -> InReview -- Ready for QA | @dev |
| 2026-07-08 | 1.1.1 | QA Gate PASS -- Status: InReview -> Done | @qa |
