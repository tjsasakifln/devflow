# Story 1.2: Consolidate Artifacts

**Status:** Done
**Epic:** 1 — Kernel Consolidation

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "tsc"]

## Story

As a **developer**,
I want **all artifact logic in `src/kernel/artifacts/` without duplicate in `src/artifacts/`**,
so that **template management has a single source of truth**.

## Scope

**IN:** `src/artifacts/` (12 templates + validator.ts) → `src/kernel/artifacts/`.
**OUT:** `src/kernel/artifacts/` (destino — já existe, precisa receber validator.ts).

**Complexity:** S (12 arquivos, 1 adição de validator)

## Acceptance Criteria

1. `src/artifacts/` removido — imports redirecionados para `src/kernel/artifacts/`
2. `validator.ts` portado para kernel
3. Template generation (requirements.md, roadmap.md, etc.) funcional
4. `devflow feature new` cria workspace com artifacts corretos

## Integration Verification

1. IV1: `devflow feature new test-epic1` cria estrutura de diretórios esperada
2. IV2: Templates gerados têm conteúdo idêntico ao pré-migração
3. IV3: `devflow status` detecta estados baseados em artifacts existentes

## Tasks / Subtasks

- [x] 1. Verificar todos os imports de `src/artifacts/` no codebase
- [x] 2. Portar `validator.ts` para `src/kernel/artifacts/validator.ts`
- [x] 3. Redirecionar todos os imports de `src/artifacts/` para `src/kernel/artifacts/`
- [x] 4. Remover diretório `src/artifacts/`
- [x] 5. Rodar `npm test` e `npm run typecheck`
- [x] 6. Testar `devflow feature new test-epic1` — verificar estrutura gerada
- [x] 7. Testar `devflow feature prompt test-epic1` — verificar templates populados

## Dev Notes

- `src/kernel/artifacts/` já existe com todos os templates exceto `validator.ts`
- Templates são reexportados via `src/kernel/artifacts/templates/index.ts`
- `devflow feature new` usa `artifacts/manager.ts` para gerar workspace
- Verificar se há imports diretos de `src/artifacts/manager` vs `src/kernel/artifacts/manager`

## Risks

Baixo — templates são reexportados. Validator.ts tem lógica de validação que precisa ser preservada.

## Testing

- `npm test`
- `npm run typecheck`
- `npx devflow feature new test-epic1` (verificar estrutura)
- `npx devflow status` (verificar detecção de estado)

## 🤖 CodeRabbit Integration

**CodeRabbit Integration**: Disabled

> Story type: Refactor (migration). No new logic — template consolidation.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 1.0.0 | Validated GO (7/10) — Status: Draft → Ready | @po |
| 2026-07-08 | 1.0.1 | Development started (YOLO mode) — Status: Ready → InProgress | @dev |
| 2026-07-08 | 1.1.0 | Development complete — Status: InProgress → InReview | @dev |
| 2026-07-08 | 1.1.1 | QA Gate CONCERNS — Status: InReview → Done | @qa |

## File List

### Created
- `src/kernel/artifacts/validator.ts` — Re-exports validators from `kernel/validators/structural.js`

### Modified
- `src/commands/feature-complete.ts` — Import redirect: `artifacts/validator` → `kernel/artifacts/validator`
- `src/commands/feature.ts` — Import redirect: `artifacts/manager` → `kernel/artifacts/manager`
- `src/commands/doctor.ts` — Import redirect: `artifacts/manager` → `kernel/artifacts/manager`
- `src/commands/init.ts` — Import redirect: `artifacts/manager` → `kernel/artifacts/manager`
- `src/commands/update-cockpit.ts` — Import redirect: `artifacts/manager` → `kernel/artifacts/manager`
- `test/unit/deprecated-imports.test.ts` — Test updated to import from `kernel/artifacts/manager`
- `test/adversarial/semantic-validation.test.ts` — Test updated to import from `kernel/artifacts/validator`
- `docs/stories/1.2.consolidate-artifacts.md` — Story status and completion

### Deleted
- `src/artifacts/` (entire directory: 19 files) — Consolidation complete, all functionality lives in `src/kernel/artifacts/`

## QA Results

### Review Date: 2026-07-08

### Reviewed By: Quinn (Guardian)

| Check | Result | Notes |
|-------|--------|-------|
| 1. Code Review | PASS | Clean mechanical port of validator.ts to kernel/artifacts/. Template re-exports intact. All imports redirected. |
| 2. Unit Tests | CONCERNS | 654/655 pass. One pre-existing timeout in `audit-engine.test.ts` (unrelated — file not modified by this story). |
| 3. Acceptance Criteria | PASS | All 4 ACs met: src/artifacts/ removed, validator.ts ported, templates functional, feature new/prompt working. |
| 4. No Regressions | PASS | All behavioral tests pass. Artifact manager resolved correctly via kernel path. Typecheck clean. |
| 5. Performance | PASS | Import redirect overhead negligible. No new computation introduced. |
| 6. Security | PASS | Templates and artifact management — no security-sensitive code. |
| 7. Documentation | PASS | Story file documents the consolidation. No external documentation impacted. |

### Gate Status

Gate: CONCERNS → docs/qa/gates/1.2-consolidate-artifacts.yaml
