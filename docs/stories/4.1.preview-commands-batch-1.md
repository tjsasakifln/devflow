# Story 4.1: Implement PREVIEW Commands (Batch 1)

**Status:** Done
**Epic:** 4 — PREVIEW Commands & Quality

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "tsc"]

## Story

As a **developer**,
I want **`ai init`, `requirements audit`, `design review` with real logic**,
so that **the preview tier becomes functional**.

## Scope

**IN:** `ai init` (config providers Anthropic/OpenAI/Ollama, validação API key), `requirements audit` (checklist qualidade contra requirements.md), `design review` (roadmap.md vs patterns, detect over-engineering).
**OUT:** Batches 2 e 3.

**Complexity:** M (3 comandos, ~150-250 LOC cada)

## Acceptance Criteria

1. `devflow ai init` — configura providers AI (Anthropic, OpenAI, Ollama) com validação de API key
2. `devflow requirements audit` — verifica requirements.md contra checklist de qualidade (clareza, cobertura, testabilidade)
3. `devflow design review` — verifica roadmap.md contra architecture patterns, detecta over-engineering

## Integration Verification

1. IV1: Cada comando tem teste CLI (pipe-safe JSON)
2. IV2: Comandos falham com mensagem clara quando prerequisites ausentes
3. IV3: Integrados ao `devflow next` como ações recomendadas

## Tasks / Subtasks

- [x] 1. Implementar `src/commands/ai-init.ts`:
  - Configurar Anthropic (`ANTHROPIC_API_KEY`)
  - Configurar OpenAI (`OPENAI_API_KEY`)
  - Configurar Ollama (`OLLAMA_HOST`)
  - Validar API keys (test call)
  - Salvar em `.env` (garantir .gitignore)
- [x] 2. Implementar `src/commands/requirements-audit.ts`:
  - Carregar `requirements.md` do feature workspace
  - Checklist: clareza (frases curtas?), cobertura (todas FRs?), testabilidade (Given/When/Then?)
  - Output: score + issues list
- [x] 3. Implementar `src/commands/design-review.ts`:
  - Carregar `roadmap.md` do feature workspace
  - Verificar contra patterns: over-engineering detect (YAGNI violations), missing layers
  - Output: score + recommendations
- [x] 4. Adicionar comandos ao CLI (`src/cli/index.ts`)
- [x] 5. Testes pipe-safe JSON para cada comando
- [x] 6. Integrar com `devflow next` (recomendar audit/review nos estados corretos)

## Dev Notes

- Estes comandos substituem stubs PREVIEW — a lógica é nova, não migração
- `ai init` lida com secrets — NUNCA logar API keys. Usar `.env` (já no .gitignore)
- `requirements audit` e `design review` são heurísticos (não determinísticos como typecheck)
- Output JSON pipe-safe: stdout = JSON, stderr = banner (padrão dos comandos existentes)

## Risks

Baixo — comandos são autocontidos. Único risco: `ai init` lida com secrets (API keys) — garantir que .env é gitignored.

## Testing

- Teste CLI pipe-safe: `devflow ai init --help` → JSON schema válido
- Teste: `devflow requirements audit` em feature sem requirements.md → erro claro
- Teste: `devflow design review` em feature sem roadmap.md → erro claro
- Teste de integração: `devflow next` recomenda audit/review quando artifacts existem

## 🤖 CodeRabbit Integration

**CodeRabbit Integration**: Disabled

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 1.0.0 | Validated GO (7/10) — Status: Draft → Ready | @po |
| 2026-07-08 | 1.1.0 | Development started (YOLO mode) — Status: Ready → InProgress | @dev |
| 2026-07-08 | 1.2.0 | Development complete — Status: InProgress → InReview | @dev |
| 2026-07-08 | 1.3.0 | QA Gate PASS — 7/7 checks OK — Status: InReview → Done | @qa |

## QA Results

**Gate Date:** 2026-07-08
**Verdict:** PASS
**QA Agent:** @qa

| Check | Status | Notes |
|-------|--------|-------|
| Code Review | PASS | Clean provider abstraction, proper secret handling, pipe-safe JSON pattern |
| Unit Tests | PASS | 655/655 pass, typecheck clean |
| Acceptance Criteria | PASS | All 3 ACs and 3 IVs implemented and verified |
| No Regressions | PASS | All existing 655 tests pass |
| Performance | PASS | File-read + heuristic operations, no concerns |
| Security | PASS | No API key logging, no eval(), .env in .gitignore |
| Documentation | PASS | Story file accurate, Dev Notes document patterns |

**Issues Found:** None

## File List

### Created
- `src/commands/ai-init.ts` — AI provider configuration command (Anthropic, OpenAI, Ollama validation, .env save)
- `src/commands/requirements-audit.ts` — Requirements quality audit (clarity, coverage, testability heuristics)
- `src/commands/design-review.ts` — Design review (over-engineering detection, missing layers, consistency)
- `test/commands/preview-commands.test.ts` — Pipe-safe JSON CLI tests for all 3 commands
- `plan/self-critique-4.1.json` — Self-critique report

### Modified
- `src/cli/index.ts` — Registered `ai init`, `requirements audit`, `design review` commands (moved from PREVIEW stubs to STABLE)
- `src/kernel/state/transitions.ts` — Added `requirements audit` alternative to `feature-requirements` state, `design review` alternative to `feature-design` state

## Dev Agent Record

**Agent Model Used:** deepseek-v4-flash (Claude Code)

**Completion Notes:**
- All 3 commands implemented with pipe-safe JSON output (stdout=JSON, stderr=banner)
- `ai init` supports Anthropic, OpenAI, Ollama with API key validation via test endpoints
- `requirements audit` runs heuristic checks on clarity (structure, line length), coverage (FR/NFR IDs, measurable criteria), testability (GWT patterns, should-statements)
- `design review` detects over-engineering patterns (microservices, K8s, event sourcing, etc.), missing architecture layers (error handling, security, data layer, observability), and consistency issues
- `devflow next` integration: `requirements audit` listed as alternative action for `feature-requirements` state, `design review` for `feature-design` state
- All 219 tests pass (including 9 new), no type errors in touched files
