# Story 3.1: Parallel Agent Spawner

**Status:** Done
**Epic:** 3 — Multi-Agent Orchestration

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "tsc"]

## Story

As a **developer**,
I want **to spawn N agents in parallel, each analyzing a different dimension of the code**,
so that **comprehensive analysis completes in wall-clock time of the slowest agent, not sum of all**.

## Scope

**IN:** Spawn de N agentes paralelos, `devflow analyze --parallel=<dimensions>`, 6 dimensões padrão, consolidação automática, limite configurável.
**OUT:** Verify pattern (3.2), Critic pattern (3.3).

**Complexity:** M (subprocess management + result consolidation)

## Acceptance Criteria

1. `devflow analyze --parallel=<dimensions>` spawna agentes isolados simultâneos
2. Dimensions: security, performance, architecture, tests, docs, deps
3. Cada agente recebe contexto focado (apenas arquivos relevantes)
4. Resultados consolidados automaticamente ao final
5. Limite configurável de agents paralelos (default: min(16, cpu cores - 2))

## Integration Verification

1. IV1: Análise paralela de 6 dimensões completa em < tempo da soma sequencial
2. IV2: Resultados consolidados sem conflitos ou duplicação
3. IV3: Timeout individual por agente não mata os outros

## Tasks / Subtasks

- [x] 1. Implementar `src/kernel/orchestration/parallel-spawner.ts`
- [x] 2. Implementar fila de spawn com limite configurável (default: min(16, cpu-2))
- [x] 3. Implementar isolamento: cada agente recebe apenas arquivos da sua dimensão
- [x] 4. Implementar 6 dimensões padrão com filtros de arquivo:
  - security: `**/auth/**, **/guards/**, **/*.secret*`
  - performance: `**/*.query*, **/*.bench*`
  - architecture: `**/src/kernel/**, **/*.architecture*`
  - tests: `**/*.test.ts, **/*.spec.ts`
  - docs: `**/*.md, **/docs/**`
  - deps: `package.json, **/import*`
- [x] 5. Implementar consolidação: merge de resultados por dimensão, dedup
- [x] 6. Implementar timeout individual (default: 120s por agente)
- [x] 7. Adicionar comando `devflow analyze --parallel` ao CLI

## Dev Notes

- Usa `child_process.fork` para agentes Node.js, `child_process.spawn` para shell
- Contexto passado via arquivo temporário (não via argv — estoura limite)
- Consolidação: cada agente retorna JSON, merger faz deep merge com dedup por file+line
- Dimensões são extensíveis: usuário pode definir `--dimensions=custom` com config file
- Workflow engine integration via guards/effects em `parallel-analysis-integration.ts`
- `devflow next --diagnose` executa análise leve paralela (security, architecture, deps)

## Risks

Médio — subprocessos paralelos consomem memória e CPU. Mitigação: limite default min(16, cpu-2), timeout individual.

## Testing

- [x] Teste unitário: spawner com mock de subprocesso (7 tests)
- [x] Teste de consolidação: 3 resultados com overlap → verificar dedup (18 tests)
- [x] Teste de timeout: agente que nunca termina → timeout, não mata outros (4 tests)
- [x] Teste de dimensões: 6 dimensões padrão, resolução, maxParallel (14 tests)
- [x] Total: 43 tests passando, 0 falhas

## 🤖 CodeRabbit Integration

**CodeRabbit Integration**: Disabled

## Dev Agent Record

**Agent Model:** DeepSeek V4 Flash / Claude Opus 4.8
**Mode:** YOLO (Autonomous)
**Total Tests:** 456 passed (all files)
**Typecheck:** Clean
**Implementation Date:** 2026-07-08

## QA Results

### Review Date: 2026-07-08

### Reviewed By: Quinn (Guardian)

**7-Point QA Checklist:**
1. **Code review** -- PASS. Clean TypeScript with proper Node.js patterns. Uses `child_process.fork`, temp files for context, semaphore-style worker pool, proper error handling, temp directory cleanup.
2. **Unit tests** -- PASS. 43 tests across 4 test files, all passing. Covers: spawner, dimensions, timeout, result consolidation.
3. **Acceptance criteria** -- PASS. All 5 ACs met: CLI integration, 6 dimensions, focused context, automatic consolidation, configurable limit default min(16, cpu-2).
4. **No regressions** -- PASS. 654/655 tests pass (1 pre-existing timeout in audit-engine.test.ts, unrelated).
5. **Performance** -- PASS. Parallel design with concurrency control. Timeout isolation prevents cascading failures.
6. **Security** -- PASS. No hardcoded secrets, no eval/unsafe patterns. Temp directory with cleanup.
7. **Documentation** -- PASS. Well-documented source, comprehensive story file with change log.

### Gate Status

Gate: PASS -> docs/qa/gates/3.1-parallel-agent-spawner.yml

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-08 | 1.0.0 | Validated GO (7/10) — Status: Draft -> Ready | @po |
| 2026-07-08 | 1.1.0 | Development started (yolo mode) — Status: Ready -> InProgress | @dev |
| 2026-07-08 | 1.1.0 | Development complete — Status: InProgress -> InReview | @dev |
| 2026-07-08 | 1.1.1 | QA Gate PASS — Status: InReview -> Done | @qa |

## File List

### Created
- `src/kernel/orchestration/types.ts` — Shared types for parallel agent system
- `src/kernel/orchestration/dimensions.ts` — 6 default dimension definitions + resolution
- `src/kernel/orchestration/parallel-spawner.ts` — Main spawner with concurrency control
- `src/kernel/orchestration/agent-runner.ts` — Fork child entry point for agents
- `src/kernel/orchestration/result-merger.ts` — Result consolidation and dedup
- `src/kernel/workflow/parallel-analysis-integration.ts` — Workflow engine guards/effects
- `src/commands/analyze.ts` — CLI command for `devflow analyze`
- `test/unit/parallel-spawner.test.ts` — Spawner unit tests (7 tests)
- `test/unit/parallel-spawner-timeout.test.ts` — Timeout behavior tests (4 tests)
- `test/unit/parallel-dimensions.test.ts` — Dimension definition tests (14 tests)
- `test/unit/result-merger.test.ts` — Consolidation + dedup tests (18 tests)

### Modified
- `src/cli/index.ts` — Registered `devflow analyze run` command
- `src/commands/next.ts` — Added parallel analysis to `--diagnose` mode
