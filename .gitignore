node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.tmp/
*.tmp

# Reversa (reverse engineering artifacts — local only)
.reversa/
_reversa_sdd/
_reversa_docs/
_reversa_forward/

# AI agent configuration (local only)
CLAUDE.md
.claude/
.agents/
.playwright-mcp/

# AIOX framework (installed tooling — not project source)
.aiox-core/
.aiox/

# External AI tools (local config — not project source)
.antigravity/
.codex/
.cursor/
.gemini/
.github/agents/
.kimi/

# Devflow (local project state — auto-generated, not part of source)
.devflow/
_devflow/
DEVFLOW.md

# Test coverage
coverage/

# Environment & Secrets (AIOX)
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules

# Build & Logs (AIOX)
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt
